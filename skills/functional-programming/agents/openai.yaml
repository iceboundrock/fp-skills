interface:
  display_name: "Functional Programming"
  short_description: "Separate effects, model states, avoid performative FP"
  default_prompt: "Use $functional-programming to make this code easier to reason about locally, without over-engineering it."

policy:
  allow_implicit_invocation: true
